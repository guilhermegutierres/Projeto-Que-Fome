{
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "cypress": "^15.14.1",
    "jest": "^30.3.0",
    "jest-fetch-mock": "^3.0.3",
    "jest-mock-extended": "^4.0.1"
  }
}
